---
layout: base.njk
title: All Articles
---

<h1>All Articles</h1>

{% if articles.length > 0 %}
  <div class="article-list">
    {% for article in articles %}
      <article class="card article-item">
        <h2>{{ article.Title or article.Heading or article._metadata.displayName }}</h2>
        <p class="meta">
          {% if article.Author %}By {{ article.Author }} | {% endif %}
          Published: {{ article._metadata.published | readableDate }}
        </p>
        {% if article.Summary %}
          <p class="summary">{{ article.Summary }}</p>
        {% endif %}
        {% if article.MainBody %}
          <div class="content">
            {{ article.MainBody | safe }}
          </div>
        {% endif %}
        {% if article._metadata.url.default %}
          <p><a href="{{ article._metadata.url.default }}" class="read-more">View on site →</a></p>
        {% endif %}
      </article>
    {% endfor %}
  </div>
{% else %}
  <p>No articles found. Make sure your Optimizely Content Graph is configured properly.</p>
{% endif %}

<p><a href="/" class="btn">← Back to Home</a></p>
